# --- Database ---
# Stringa di connessione MongoDB (Atlas o locale)
MONGODB_URI="mongodb://localhost:27017/subsmate"
MONGODB_DB="subsmate"

# --- App ---
NEXT_PUBLIC_APP_NAME="SubsMate"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Autenticazione ---
# Segreto per la firma dei cookie di sessione (32 byte casuali, base64url).
# Generato automaticamente da execution/seed_admin.py se assente in frontend/.env.local.
AUTH_SECRET=""

# --- Associazione (usata nei documenti/export) ---
ORG_NAME="Nome Associazione APS"
ORG_FISCAL_CODE=""
ORG_EMAIL=""

# --- Script di esecuzione (execution/) ---
# Base URL usato dagli script Python per parlare con le API Next.js
API_BASE_URL="http://localhost:3000/api"
